/*jslint browser: true*/
/*global $scope, angular */
define(['angular'], function(angular) {
  'use strict';
  var moduleControllers = angular.module('login-controllers-form', []);

  moduleControllers.controller('login-controllers-form',['$scope',
    function($scope){

    }
  ]);
});
